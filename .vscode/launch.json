{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug index.html (NO server)",
            "type": "pwa-chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
